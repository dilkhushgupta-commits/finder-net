# Frontend Environment Configuration
# In production, leave VITE_API_URL=/api (nginx proxies to backend)
# In development, Vite proxy handles /api -> http://localhost:5000
VITE_API_URL=/api
VITE_SOCKET_URL=
VITE_APP_NAME=Finder-Net
